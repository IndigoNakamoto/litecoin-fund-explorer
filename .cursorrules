# litecoin-fund-explorer

Canonical Cursor rules for this app: **[`.cursor/rules/litecoin-fund-explorer.mdc`](../.cursor/rules/litecoin-fund-explorer.mdc)** (monorepo root).

Open the **Migrate** workspace root so Cursor loads all `.cursor/rules/*.mdc` files.
